// Main entry point for Rust MQTT comparison
// This is a placeholder - the actual binaries are in publisher.rs and subscriber.rs

fn main() {
    println!("MQTT Rust Implementation");
    println!("Use 'cargo run --bin publisher' or 'cargo run --bin subscriber'");
}
